import React, { Fragment } from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';
import {
  Title,
  Subtitle,
  Body,
  Micro,
  BodyConfig,
  TypographyConfig,
} from '@vds/typography';
import { colors } from '@vds/theme';
import { IconData } from '@vds/icons';
import { TrailingTooltip as CoreTrailingTooltip } from '@vds-core/tooltips';

const TrailingTooltip = props => {
  return (
    <CoreTrailingTooltip
      {...props}
      Body={Body}
      Title={Title}
      Micro={Micro}
      Subtitle={Subtitle}
      colorConfig={colors}
      bodyConfig={BodyConfig}
      iconConfig={IconData}
    />
  );
};

export default TrailingTooltip;
