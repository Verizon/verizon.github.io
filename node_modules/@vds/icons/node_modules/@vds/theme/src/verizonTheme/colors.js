import { colors as oldColors } from '@vds-core/theme';
const newColors = {
  blueInverted: '#0088CE',
  blue: '#0077B4',
  green: '#008330',
};
const colors = { ...oldColors, ...newColors };
export default colors;
