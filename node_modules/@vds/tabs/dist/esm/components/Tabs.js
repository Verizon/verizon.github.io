import _extends from "@babel/runtime/helpers/extends";
import React, { Component } from 'react';
import { Tabs as CoreTabs } from '@vds-core/tabs';
import { TitleConfig, TypographyConfig } from '@vds/typography';
import { colors } from '@vds/theme';
var typescale = TypographyConfig.getTypescale();

var Tabs = function Tabs(props) {
  return /*#__PURE__*/React.createElement(CoreTabs, _extends({}, props, {
    focusState: true,
    minHeight: true,
    colorConfig: colors,
    titleConfig: TitleConfig,
    typescale: typescale
  }));
};

export default Tabs;